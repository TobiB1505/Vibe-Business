# Architecture Decision Records (ADRs)

This directory holds Architecture Decision Records: short documents that capture a significant architecture or product-behavior decision, the context that led to it, and its consequences, at the time it was made.

## Why

[ARCHITECTURE.md](../../ARCHITECTURE.md) intentionally lists a number of open decisions rather than pretending they are already settled. As each of those (or other significant decisions) is actually made, it should be recorded here — not only discussed in chat — so the reasoning survives independently of any single conversation. See [CLAUDE.md](../../CLAUDE.md) rule 13.

## When to write an ADR

Write one when a decision:

- Is hard or costly to reverse, or
- Affects multiple layers/modules described in [ARCHITECTURE.md](../../ARCHITECTURE.md), or
- Resolves one of the open decisions explicitly listed in ARCHITECTURE.md, or
- Meaningfully changes product behavior described in [PRODUCT.md](../../PRODUCT.md).

Small, easily reversible implementation choices do not need an ADR.

## Naming

Sequential, numbered, kebab-case, descriptive:

```
0001-github-as-integration-layer.md
0002-preview-deployment-strategy.md
```

## Status

V0.1 foundational architecture decisions have been recorded:

- [0001](0001-modular-monolith.md) — Modular Monolith with Next.js + TypeScript (Accepted)
- [0002](0002-supabase-postgres-and-auth.md) — Supabase Postgres and Supabase Auth (Accepted)
- [0003](0003-github-app-integration.md) — GitHub App as Repository Integration Layer (Accepted)
- [0004](0004-vercel-as-initial-host-and-preview-provider.md) — Vercel as Initial Host and Preview Provider (Accepted)
- [0005](0005-ai-provider-abstraction.md) — AI Provider Abstraction, Anthropic First (Accepted)
- [0006](0006-untrusted-repository-execution.md) — Untrusted Repository Execution (Accepted principle / Deferred provider)
- [0007](0007-audit-log.md) — Postgres Append-Only Audit Log (Accepted)
- [0008](0008-secrets-management.md) — Secrets Management (Accepted)
- [0009](0009-github-installation-ownership-verification.md) — GitHub Installation Ownership Verification (Accepted)
- [0010](0010-safe-outbound-http-inspection.md) — Safe Outbound HTTP Inspection (Accepted)
- [0011](0011-ai-inference-and-evidence-trust-boundary.md) — AI Inference and the Evidence Trust Boundary (Accepted)
- [0012](0012-authenticated-browser-analysis.md) — Authenticated Browser Analysis (Accepted)
- [0013](0013-durable-operation-execution.md) — Durable Operation Execution (Accepted)
- [0014](0014-first-execution-safety.md) — First execution is isolated, premise-revalidated and capability-scoped (Accepted)
- [0015](0015-untrusted-repository-execution-provider.md) — Untrusted Repository Execution Provider: Vercel Sandbox (Accepted)
- [0016](0016-temporary-preview-isolation.md) — Temporary Preview Isolation (Accepted)
- [0017](0017-visual-review-artifacts.md) — Visual Review Artifacts (Accepted; §9 corrected by the first dogfood)
- [0018](0018-human-approval-authority.md) — Human Approval Authority (Accepted)
- [0019](0019-safe-approved-change-merge.md) — Safe Approved Change Merge (Accepted)
- [0020](0020-production-outcome-verification.md) — Production Outcome Verification (Accepted)
- [0021](0021-business-outcome-measurement.md) — Business Outcome Measurement (Accepted)
- [0022](0022-sentry-observability.md) — Sentry for Error Monitoring and Baseline Tracing (Accepted)
- [0023](0023-project-scoped-onboarding-orchestration.md) — Project-scoped Onboarding Orchestration (Accepted)
- [0024](0024-vibe-credits-economic-layer.md) — Vibe Credits as an Internal Economic Layer (Accepted)
- [0025](0025-stripe-payment-rail-and-credit-grants.md) — Stripe as Payment Rail, and Credit Grants as Provenance (Accepted)
- [0026](0026-agentic-execution-contract.md) — Agentic Execution Contract (Accepted)
- [0027](0027-coding-agent-provider-and-tool-gateway.md) — Agentic Coding: Provider Abstraction, Tool Gateway, and Independent Validation (Accepted)
- [0028](0028-founder-selectable-action-plan-move.md) — Founder-Selectable Action Plan Move (Accepted)
- [0029](0029-agent-runtime-placement-and-credential-broker.md) — Agent Runtime Placement: the Harness in the Sandbox, the Key Behind a Gateway (Accepted, amends 0027)
- [0030](0030-agent-execution-observability.md) — Agent Execution Observability: one ordered event log, a reusable live view, derived economics (Accepted, extends 0027 and 0029)
- [0031](0031-execution-context-intelligence.md) — Execution Context Intelligence: verify relevant facts, do not broadly rediscover (Accepted, extends 0027, 0029 and 0030)
- [0032](0032-agent-verification-and-completion.md) — Agent Verification and Completion: the agent checks enough to converge, the validator checks enough to authorize (Accepted, extends 0027, 0029 and 0031)
- [0033](0033-post-implementation-completion-control.md) — Post-Implementation Completion Control: stop paying for exploration after the job has converged (Accepted, extends 0027, 0029, 0031 and 0032)
- [0034](0034-execution-surface-and-lifecycle.md) — Execution surfaces, and separating implementation breadth from convergence (Accepted, supersedes parts of 0031 and 0033)
- [0035](0035-commit-message-compiler.md) — Commit Message Compiler: Conventional Commits from the trusted Action Step (Accepted, narrows Rule 57's application to the Planner's own already-trusted text)
- [0036](0036-risk-adaptive-validation-depth.md) — Risk-Adaptive Independent Validation Depth: profile answers *which commands*, depth answers *how many this change deserves* (Accepted, extends Rule 65 with a second versioned axis; leaves Rule 66 untouched)
- [0037](0037-automatic-validation-and-review-classification.md) — Automatic validation hand-off, and deterministic review classification (Accepted, establishes that one durable operation may enqueue the next; leaves the visual-review "nothing is automatic" rule intact)
- [0038](0038-economy-intelligence-layer.md) — The Economy Intelligence layer (Accepted, extends 0024; predicts what a run will cost from pre-execution signals only, measures how wrong it was, and activates nothing — `CREDIT_RATE_CARDS` stays empty)
- [0039](0039-documentation-currency.md) — Where truth lives, and how documentation stays current (Accepted; one authoritative home per claim, records are immutable and current-state documents must be true at HEAD, and the structural half is asserted by `src/lib/docs/documentation-currency.test.ts`)
- [0040](0040-ci-hosted-database-concurrency-gate.md) — Where a real-database concurrency test runs, and what it may reach (Accepted; a disposable local Supabase stack on a GitHub runner, no secret of any kind, no PostgreSQL driver, and a target guard that makes the deployed project unreachable by construction rather than by policy)
- [0041](0041-marketing-attribution-pixel.md) — The Meta Pixel, and the two boundaries it runs inside (Accepted; production deployments only and public pages only, so the identifiers in `/app` paths never reach an advertising network — `PageView` and nothing else)
- [0042](0042-billing-reconciliation-authority.md) — Billing Reconciliation Authority: one terminal-CAS authority doctrine, one idempotent materialization primitive shared by the hot path and repair, and a certified, drain-gated cutover, closing drift repair, orphaned holds, stranded lot capacity and zero-credit settlement idempotency (Accepted; design only, no implementation)
- [0043](0043-data-api-privilege-model.md) — Where the Data API's privileges come from (Accepted; the repository states its own PostgREST grants per table and per role instead of inheriting a platform default that is removed 2026-10-30 — explicit grants and revoked default privileges shipped, the tightening of the existing tables' surplus deliberately separate)
- [0044](0044-evidence-pack-v4.md) — What `business-evidence.v4` is for (Accepted; scope decided, not implemented — the id-polarity migration and a `contradiction.*` namespace land in one bump rather than two, because each alone invalidates every audit identity, and the implementing sprint must widen `verifyPackProvenance`'s exact-v3 discriminator or silently lose the stronger provenance path)
- [0045](0045-command-center-information-architecture.md) — The project workspace is a command center, not an admin panel (Accepted; seven sections named for the product's own Understand → Diagnose → Prioritize → Plan → Execute → Measure model, two reachable subsections deliberately outside the rail, and the `business-audit` section id held fixed so the opportunity engine's only way out of a blocked set keeps resolving)
- [0046](0046-account-dashboard-and-context-swap.md) — The account level is a dashboard of its own, and the sidebar swaps context (Accepted; a `(account)` route group makes the two rails mutually exclusive by construction rather than by a conditional, the hero names one product because there is no honest account-level score, the trend's comparability comes from the seven reproducibility columns already on `business_readiness_audits` so no migration was needed, and a browser-counted density budget holds the screen to 36 elements)
- [0047](0047-business-health-is-project-home.md) — Business Health is the canonical project Home (Accepted; six rail destinations, the existing `business-audit` recovery anchor now resolves at the project root, `/health` remains a guarded compatibility alias, and the Business Brain is the one bounded theatrical product surface)
- [0048](0048-signature-business-brain.md) — Signature Business Brain view model and interaction (Accepted; bounded domain-to-view-model projection, truthful absent lens scores, comparable overall history, Motion-for-React interaction and reduced-motion parity)
- [0049](0049-business-lens-diagnostic-scores.md) — Evidence-grounded business-lens diagnostic scores (Accepted; nullable 0–100 lens scores, validated health-band consistency, legacy audit compatibility, and no change to the authoritative overall score)
- [0050](0050-lenses-are-the-audit.md) — Lenses are the audit's only framework (Accepted; the five dimensions leave the contract, the overall score is the mean over scored lenses behind a coverage threshold, stored v6/v7 audits stay valid under their own contract; supersedes ADR 0049's no-sixth-overall rule)
- [0051](0051-project-shell-context-ownership.md) — Project shell context ownership and scroll model (Accepted; product identity and switching live in the fixed rail, route headers stay in document flow, project and account settings are separated, and the main project document owns desktop scrolling)
- [0052](0052-durable-product-scan-discovery-feed.md) — Durable Product Scan and bounded discovery feed (Accepted; repository reading, public-product inspection and Product Understanding are one durable operation, with a 24-event append-only feed and one shared Motion surface in onboarding and My Product)
- [0053](0053-founder-input-resolution.md) — Founder-owned input resolution and Action Plan completion evidence (Accepted; dynamic planner-generated requests resolve into versioned project context, complete founder-owned steps from evidence, and feed immutable execution specs without inventing pause/resume)
- [0054](0054-agent-action-plan-completion-evidence.md) — Agent Action Plan completion comes from verified execution evidence (Accepted; immutable step binding, successful planner run, Vibe candidate verification and independent validation jointly complete Agent work without a mutable completion flag)
- [0055](0055-founder-action-attestation-evidence.md) — Founder actions complete from explicit immutable attestation (Accepted; an owned founder_action step completes only from criterion-bound, audit-ready founder testimony, never a universal checkbox)
- [0056](0056-lifecycle-erasure-and-retention.md) — Lifecycle, erasure and retention (Accepted; Disconnect and Delete become two honest operations, four verbs classify every table, the identity is deleted while the billing graph is tombstoned whole, the ExecutionSpec carve-out makes the `DELETE` privilege the authority and the GUC only a context marker, and the intra-project RESTRICT foreign keys the audit blamed are empirically cleared and left alone)
- [0057](0057-account-level-durable-operations.md) — Account-level durable operations, and how an erasure outlives itself (Accepted, extends 0013 and unblocks 0056 §4; `operation_runs` gains a nullable project, a tombstoned owner, an account-level uniqueness index and a start-path trigger, so the eleven-step erasure can be the single durable operation it was specified as)
- [0058](0058-move-focus-url-contract.md) — Move focus is a shared URL contract, never authority (Accepted; `?plan=` names which Move a surface is about, is sanitized and resolved against stored Moves before it means anything, degrades to the unfocused page rather than substituting rank 1, carries no authority to act, and is carried onward by exactly one navigation item)
- [0059](0059-security-response-headers.md) — Security response headers, and a CSP that starts by watching (Accepted; VB-005. Six headers on every response and `X-Powered-By` gone, with the Content-Security-Policy deliberately report-only per the launch gate's own sequencing — a first policy written from reading the code is a guess, and an enforced guess breaks whatever its author missed. Records the two directives that are weaker than a textbook policy and why: Next's inline bootstrap scripts, and customer brand logos rendered from arbitrary origins. States plainly that a report-only CSP protects nothing until enforced)
- [0060](0060-sign-in-throttle-authority.md) — The sign-in throttle's authority is who may call it (Accepted; VB-053. `record_auth_attempt` was granted to `anon` because sign-in precedes a session — correct about the caller, wrong about the consequence: `anon` is anyone holding the publishable key, and eight forged failures held a known address out of sign-in for fifteen minutes at a time, repeatably. The control used as a weapon. `execute` is revoked from both Data API roles and the only caller is a service-role client obtained inside `throttle.ts`; the authority moved from what a caller can prove about its arguments to whether it may call at all. Records why a server-held secret and dropping the throttle were both rejected, why the JWT-derived clear is replaced rather than kept, and — corrected after reading `pg_proc.proacl` back — that `service_role` needs the grant explicitly or the throttle fails open in silence)
- [0061](0061-launch-v1-operation-rate-card.md) — What a Credit buys, and what each number is worth trusting (Accepted; satisfies rule 78. `launch-v1` prices every customer-facing operation and takes effect at the same instant Sonnet 5's scheduled +50% does — one event, because the card exists to hold the margin those rates move: at the Pro plan's credit value `retail-v1` would have fallen from ~80% to ~68% overnight with no code change and every test green. Audit 35→55, Next Moves 20→30, Action Plan 15→30; Deep Scan and Agentic Execution priced for the first time, the agent per execution class at 150/200/350. Plans and packs deliberately unchanged, so no Stripe Price moves. Every entry carries a `PriceBasis` because the card holds three different kinds of claim: three `measured`, the Agent `modelled` — `complex` has zero cost observations — and Deep Scan `policy`, the only price with no measured cost behind it at all. `margin-guard.ts` recomputes each margin from the provider rates in force and fails below 70%, so the next rate change is a red test rather than a discovery made later; it names the three prices it cannot check. `CREDIT_RATE_CARDS` stays empty, and the ADR records why that is a different question)
- [0062](0062-sonnet-5-price-rise-cancelled.md) — A cancelled provider price is deleted, not held (Accepted; amends 0061. Anthropic withdrew the Sonnet 5 rise to $3/$15 before it took effect — verified 2026-08-31 against three first-party pages — so `ai/pricing.ts` carries one open-ended row at $2/$10 and the September row is *deleted* rather than commented, flagged or pushed forward: a dead future price inside an array a resolver walks is one edit from pricing real usage, and it protects nothing because it never billed a call. `pricingVersion` still says "introductory" for a now-permanent price, because 1,050 settled rows name that string and the version answers which prices were in force, not what the provider called them. A permanent temporal regression test asserts identical pricing either side of the cancelled instant and exactly one Sonnet row at neither $3 nor $15; reintroducing the row fails six assertions. `launch-v1` is re-derived rather than reverted — Audit 55→35, Next Moves 30→20, Action Plan 30→**20**, which is *not* `retail-v1`'s 15 because that came from n=1 and returns 74.5% against a 20% target cost share. Deep Scan and the agent tiers deliberately unmoved, for opposite reasons. The guard is preserved and given teeth: a pricing-injection seam, a 2× synthetic card that breaches the floor, and `TARGET_COST_SHARE` / `ASSUMED_EUR_USD` declared once and reported. No migration, no Stripe change, no history re-rated. Recalibrated, not validated — three of eight prices still rest on something other than measurement)
- [0063](0063-review-classification-as-a-gate.md) — The review classification becomes a gate, and a diff becomes approval evidence (Accepted; supersedes ADR 0037 §2's "deterministic and advisory", amends 0018. `classifyReview` decided nothing while `change_approvals.review_artifact_id` was `not null`, so a change altering no rendered page could only be approved after the user paid for a preview sandbox and a browser session to photograph a page that did not change. An approval now names exactly one evidence form and the classification decides which: a `review_artifacts` row, or a `code_review_digest` — sha256 over two immutable commits, the shown paths and the diff policy version. That binding is *stronger* than the screenshot one, which is the argument the ADR rests on: two commits reproduce a diff byte for byte indefinitely, while review images expire at seven days and their "before" side is production as it was and can never be regenerated. Two database CHECKs hold the shape — exactly one form, and a diff digest only on `code`. The merge gate deliberately never re-asks the classification: it reads the form off the approval row, because the analyzer's route table moves and rule 68 says drift never rewrites what a human decided; where the requirement genuinely changes, a fourth invalidation reason says so. `visual_and_code` keeps the visual requirement, and an undeterminable classification resolves to the stricter path unchanged. Also builds the diff that was missing: base-vs-head at two pinned commits, a line diff written rather than installed, nothing persisted, `deleted` deliberately not a status because `getTextFile` cannot tell absence from a binary or oversized file)
- [0064](0064-preview-before-validation.md) — The preview comes before the check, and serves the commit (Accepted; supersedes ADR 0016 §1, §3, §7 and §11. A preview restored the filesystem snapshot a *passing* validation captured, and validation's last step is the build — measured at install 18s → typecheck 79s → test 84s → build 99s, so a person waited about five minutes to look at code that was already finished, then paid again for restore and an integrity recheck. A preview now clones the prepared commit into its own sandbox and runs `next dev`, alongside validation rather than after it. Its own sandbox because `ports` are settable only at `create()` — borrowing validation's would mean exposing unchecked code on a public URL from the first second — and two durable steps because a `SandboxProcess` cannot cross a step boundary. All four egress transitions are preserved: SOURCE_HOSTS to clone, DEPENDENCY_HOSTS to install, `deny_all` before any repository-controlled command, and the credential scrubbed and its absence verified first. What the preview *claims* changes with it, from "the validated application is running" to "the prepared code is running", and the product says so wherever one is offered. The `ValidatedArtifact` is switched off entirely: no snapshot, no restore, no integrity recheck, and no 24-hour retention of a customer's file tree at the provider. `preview-policy-v2`, with the identity keyed on the commit that was served rather than on a snapshot id. Three assumptions are reasoned rather than observed and need a dogfood run: that `next dev` starts under `deny_all` with an exposed port, that the first-request compile fits 180s, and that the public route for port 3000 exists with no prior build)
- [0065](0065-the-preview-is-the-review.md) — The preview is the review (Accepted; supersedes ADR 0017 in its role as a gate, amends 0063 and 0018. A `visual` change was approved on two PNGs of `REVIEW_POLICY.route = "/"` at 1440×1000 — a strictly poorer instrument than the running application they were photographs *of*, and Vibe paid a browser session for the reduction. An approval now binds to the `preview_sessions` row of the same commit plus the code diff: the sandbox is gone in fifteen minutes, the row is immutable and says an interactive preview of exactly these bytes ran and became reachable at `ready_at`. The evidence is resolved from server state — same project, same change, same commit, `ready_at` set — and the *earliest* such session, not the newest, so that starting a second preview to look again cannot silently move what an approval binds to or invalidate a standing one. The diff is in every new form, so a visual approval binds to strictly more than before. An undeterminable classification takes this path too, because nothing creates a comparison any more and the old fallback would make an unclassifiable change permanently unapprovable. The screenshot path is unreachable rather than deleted — its server action is removed, its read paths stay for historical rows — and its retention sweep moves to preview teardown, since a workflow nothing starts sweeps nothing. The "before" half is a link to the customer's live site, labelled *now* rather than *before*. What is lost, stated plainly: the comparison is no longer conserved, and fifteen minutes after a preview ends there is nothing left to look at. Repairs a Sprint 0113 defect on the way: the insert policy's unconditional comparison clause meant `ra.id = null` matched no row, so the code-diff form would have been refused by RLS in every customer session while passing every owner-role test)

- [0066](0066-payment-meaning-across-evidence-families.md) — Payment meaning is read from every evidence family that can carry it (Accepted. `classifyExecutionRisk` refuses payment work outright — "at any risk tolerance" is the constant's own wording — but `surfaceIdOf` stripped only `repo.surface[_absent].*` and `live.surface[_absent].*` and read everything else as "no financial meaning". Two families that routinely carry it were invisible: `repo.integration.<id>` (`stripe`, `paddle`, `lemonsqueezy`) and `auth.surface.<id>[_not_observed]` (`billing`). A real plan step — *"Wire the pricing page to a working Stripe checkout and surface billing to signed-in users"* — cited two of them and classified `moderate`, inside `MAX_AGENTIC_V1_RISK` and eligible for an agent. The same defect Sprint 0073 refused once in the other direction, one namespace over. Risk now resolves a `RiskMeaning` per id across all three families, with the integration category **derived** from the detector catalogue so a provider added there is refused here without anyone remembering this gate exists; polarity stays ignored, and classification stays keyed on our ids and never on prose. `auth.` is deliberately not an entry in `SURFACE_NAMESPACES` — that record is a pair of prefixes and three consumers iterate it as such, while the signed-in pack expresses absence as a suffix. `EXECUTION_RISK_POLICY_VERSION` becomes v2 because it is an input to `computeExecutionSpecIdentity` and a v1 spec was measured against a gate that could not see either family (rule 65). Two non-widenings are pinned by tests rather than left to judgement: a pricing page stays `moderate` — it is marketing copy, and prohibiting it would refuse the only Move with an agentic route — and signed-in surfaces other than billing stay unescalated, since `auth.` names where the Deep Scan stood, not what the step changes)

- [0067](0067-plan-screen-renders-the-resolver.md) — The plan screen renders the execution resolver, not only the stored classification (Accepted. Three independent answers to "can Vibe do this" exist and the plan screen rendered the weakest: `executionSupport` is derived from the deterministic capability registry alone — one entry — so a real step, *"Build a public pricing page for the confirmed plans"*, was stored `not_yet_supported` and read as "Vibe's work / Not automated yet" while `resolveStepExecution` classified the same step `agentic` and the Agent workspace offered a Run button for it. Both sentences were true of the same step at the same time. For `not_yet_supported` steps only, the screen now renders the resolver's answer through one pure function; `intrinsicMode` rather than `mode`, because a step waiting on a prerequisite is still a step the agent could build and the row already prints its own waiting line. The route resolves it with the allowlist lifted off — "what could Vibe build?" is owed to every founder, while "may you start it now?" stays behind the allowlist, the Credits and admission — and reads state only, no live HEAD and no crawl. The deterministic path is untouched: `isExecutableByVibe` unchanged, `vibe_prepares` keeps its own sentence, and the forbidden-control sweep still finds nothing, because a copy change that quietly became a button would be worse than the defect. Adding an agentic value to `ExecutionSupport` was rejected: a CHECK migration, a dependency from `classify.ts` onto repository state at plan-generation time, every persisted row still wrong — and a stale answer written into a durable column, which is the failure being removed rather than relocated)
- [0068](0068-retention-periods.md) — Retention periods, by what the data is for (Accepted. Closes [ADR 0056](0056-lifecycle-erasure-and-retention.md)'s deferred P-2, which forbade inventing a duration in an implementation. **First it separates two problems PERF-018 put in one finding**: `max_rows = 1000` truncating an unbounded read is a *correctness* defect — a well-formed wrong answer to a sum — and deleting old rows only moves the date it starts being wrong, so completeness reads must aggregate or paginate whatever the period is. Retention answers cost and privacy, and nothing else. **Four classes, because there are four regulators**: financial records ten years (HGB §257 / AO §147 statutory minimum, which GDPR Art. 17(3)(b) does not shorten — which is why erasure tombstones this graph); the audit trail eighteen months, so an incident noticed late still has a full prior year behind it; operational event streams ninety days, the highest write rate and the lowest retained value; and derived intelligence by **count, not age** — the latest three per project, because an age rule would delete a quiet project's only audit and leave a founder reading "not analysed yet" for work they paid for. `ai_usage_events` deliberately follows the financial class rather than the operational one, because ADR 0056 §7 already established that deleting metering while keeping the charge makes "why was this account charged" unanswerable. **The sweep for the financial class is not a row-age sweep**: ADR 0056 F5 proved partial deletion has no repair path, so it may act only on an already-tombstoned account and only on the whole graph at once. Periods are named constants in code — not environment variables, not a dashboard — because a period changeable without a diff makes ADR 0056 §6's own rule unenforceable. **Left open on purpose**: whether a Credit ledger row is a `Buchungsbeleg` is a tax advisor's determination, so ten years is adopted as the safe direction — keeping a commercial letter too long is lawful, destroying a booking voucher early is not)
- [0069](0069-retention-sweep-trigger.md) — What deletes the expired rows, and what it may not touch (Accepted. Closes [ADR 0068](0068-retention-periods.md)'s deferred D-2, which decided four retention periods and deliberately no mechanism — so nothing deleted anything and the policy was a document. **The option 0068 nominated first was evaluated and fails**: read-triggered sweeping reaches only rows somebody reads, and nobody reads a ninety-day-old event, while any activity-amortised variant never fires for an account that has stopped being active — the exact case Art. 5(1)(e) is about. Retention needs a clock. **`pg_cron` over Vercel Cron**, because deleting rows is a database operation and the alternative's cost is an authenticated internet-reachable endpoint whose only job is to destroy data; it also keeps running while the app is paused or mid-deploy. The job is scheduled by `cron.schedule` **inside a migration**, so the repository states the schedule and a job unscheduled in the dashboard is a detectable divergence rather than a silent one; the function is `security invoker` with `execute` revoked from every Data API role, so cron is its only caller. The periods stay in TypeScript and the SQL is a second copy pinned by a test that reads both files as text — the instrument Sprint 0118 built for the poll interval. **Writing it found three things that made ADR 0068 §5 unsafe as worded**: `operation_runs` is the parent of six cascade edges, so an age sweep of it would delete the `prepared_changes`, `review_artifacts` and `validation_runs` a human approval binds to under rule 67 — out of scope at any period; `sandbox_usage_events` and `review_browser_usage` are billing sources that reconciliation projects into `billing_usage_events`, so they follow the financial class rather than the ninety-day one; and `agent_execution_events` is the only record behind rule 78's measured agent cost, which does not change its period but obliges the economy module to state its rolling horizon so an analysis cannot read a truncated window in silence. Two classes are enforced, two are not — the financial sweep cannot delete anything before 2036 and is not written, the count-based derived rule needs a different instrument — so the privacy-page disclosure lands per class as each becomes true, because a published period nothing honours is a false statement rather than a gap)
- [0070](0070-the-sandbox-is-the-boundary.md) — The sandbox is the boundary; the tool gateway is retired (Accepted; supersedes 0027's second half. ADR 0029 moved the harness into the execution's own microVM, where it edits files with its own tools and never calls back — and the tool gateway was not removed. It was constructed on every run, handed to the provider as `invokeTool` that `sandbox-runtime/provider.ts` never references, and wrote six zeros and two empty lists per run with a comment at the call site saying the trail is "empty by construction" while writing it anyway. Three consequences had accumulated: the suite's picture of a run was the retired topology, because `fakeDetachedAgentProvider` called `invokeTool` and nine test sites across three files asserted a brokered trail production has not written since 0029; a dead fallback in `extractAndVerifyStep` read brokered write paths out of the empty `agent_tool_events`, so its only possible answer was `agent_produced_no_change`; and two argv defects sat on the unreachable path, neither live nor fixed. What bounds the agent is now stated twice and nowhere else — an absent capability before the run (the harness's tool set is named explicitly, no WebFetch, no WebSearch, no MCP, asserted by `program.test.ts`) and Vibe's own refusal after it (`verifyCandidateChange`, whose test already said it refuses a forbidden path *"even if the gateway somehow allowed it"*). Rule 76 applied to its own subject: five methods nothing invoked were five denials that refused nothing. ~2,700 lines deleted. `observedPaths` becomes required and a null observation is refused as `sandbox_lost` rather than read as "nothing changed". States plainly what is given up: there is no pre-execution policy check inside the VM, which was already true and is now the design rather than a side effect. The four counter columns are kept holding historical values, because dropping a column the deployed code still selects is the skew the deployment doc forbids)
- [0071](0071-agentic-outcome-verification.md) — Outcome verification for agentic changes: routes Vibe observed, not intent it was told (Accepted; extends 0020, reverses nothing in it. `agentic_execution_v1` mapped to `null`, so every change the coding agent produced resolved to `outcome_not_supported` and the measurement half of the product was wired to two SEO generators. The comment recording that was right that no verifier can know what an agentic change was *supposed* to do — rule 57 closes the `doneWhen` escape — and skipped a third option: Vibe already holds two observations of its own, the changed paths it verified off the commit (rule 77) and the analyzer's route table for the pinned snapshot, and their intersection is a fact about *this* change. **Measured first**: 9 of 10 recorded agent runs resolve a `public_pages` surface. One check kind, `public_route_serves_page`, meaning one sentence — this path answered as a page, at the path Vibe asked for. **The content comparison the plan called for is absent, not weakened**, and the ADR records why so it is not proposed again: verification is founder-started so there is no pre-merge moment to fetch a baseline in, and a page hash is unstable across two consecutive fetches of an unchanged page — CSRF tokens, timestamps, A/B assignments — so "the content differs" would pass on a site nothing was deployed to, a false `verified`. So this establishes **reachability, never arrival**: a 200 is equally consistent with the old build serving. The value is the failure direction — a merged change that takes a public page to a 500. Only a 5xx or a page route serving not-a-page reads as `failed`; 401, 403 and 429 stay `error`, because a bot-blocking WAF answering our user agent is not the customer's product misbehaving. The limit is written in four places rather than remembered: the scope note, `OutcomeCard.profileNote` as a field, every panel state, and a check label that says "answers" rather than "works". A backend-only change refuses with `outcome_no_public_surface` — a different sentence from `outcome_not_supported`, which would now be false. Bounded at four routes, re-applied at observation time; dynamic routes and authenticated pages are never probed. Arrival stays open and needs a signal about which build is serving, which means a deployment provider and its own ADR)
- [0072](0072-the-evidence-behind-the-ceiling.md) — The evidence behind the ceiling: the estimator informs, it does not price (Accepted; amends 0038, changes no price and activates no rate card. `economy/intelligence/` was built without a consumer and had none for eight sprints; the choice was keep it with one or delete it. **Measured before deciding, because the audit's figure was wrong**: 1,096 of its 2,894 lines were already used by the calibration report, so the genuinely unreached part is 1,798. The obvious consumer — replace the class ceiling above Run with Vibe with a predicted number — is ruled out twice. `quote-simulation.ts` had already written down why a cost-derived Credit figure is unstable in the wrong direction: `credits = cost * factor` makes a quote for the same work move every time the repository grows, which is what run #6 → #9 did at 2.16x for an identical step. And the estimator's leave-one-out backtest is **24.3% mean absolute error across 7 runs, worst case +51%, with `repositoryContextAvailableFor: 0`** — its repository multiplier has never been validated against an outcome. So the estimate is consumed for its **structure and never its magnitude**: the ceiling stays the number, and beneath it go how many comparable runs stand behind it and what about this run pushes toward the top of it. `RunForecast` has four fields and none is money; a driver is two closed enums, so the estimator's own `detail` strings ("complexity 1.34x against the reference repository") stop at the boundary. **The guard is narrowed, not removed**: `sprint-0054-safety.test.ts` still forbids the estimator reaching the execution path, and now names one boundary file whose permission is conditional on two new tests — its source may contain no amount, and `RunForecast` must export exactly the four reviewed fields. Both were verified to fail before passing. `quote-simulation` and `safety-margin` stay unreadable by anybody. The pre-run screen is also the first caller that can hand the estimator a repository, projected through the compiler's own derivation so the forecast and the run's brief cannot disagree about the tree. **Three files are left without a consumer on purpose** — safety-margin (charging a customer for Vibe's uncertainty is nobody's decision yet), growth-simulation (a planning tool whose home is `docs/business/`) and variance-explanation (it needs a post-run economics surface that does not exist) — because manufacturing consumers for them is the failure this ADR argues against)
- [0073](0073-the-charge-lands-on-what-was-sold.md) — The charge lands on what was sold, and the ledger fills itself (Accepted; amends 0061's settlement timing, no price moves. **Read out of production**: run `c462c083` settled 100 Credits at `00:31:35` and began validating at `00:31:38` — the customer was charged three seconds before a single check ran, and `refundCharge` has zero callers, so a failing validation would have left them paying in full for a change Vibe refuses to ship. `credits/retail.ts` already said what the price is for: validation carries no price of its own because *"a customer bought a validated improvement, not a pipeline"*. **So the agent's hold now survives the run and the verdict resolves it** — pass settles, fail releases, a validation that cannot start releases, a stale sweep releases. One function owns it, reached through persisted rows only, idempotent by construction because settle refuses a settled reservation and release refuses a non-active one. "Could not start" cannot mean "this repository has no validation": eligibility refuses admission on `validation_not_supported` before a run begins, so it is Vibe's own fault and the approved failure policy absorbs it — no new pricing policy is invented anywhere. Discard is deliberately untouched: a validated improvement the founder does not merge was still delivered. **Two further findings from the same reading.** The sandbox half of every run's cost was never in the ledger — `provider_cost_usd` is null in all 63 rows and correctly so, but `VERCEL_SANDBOX_RATES` has been founder-attested since 2026-08-20 and the derived figure for that run is ~$0.10 against $0.754 of model spend, an eighth of it invisible. It now lands in columns of its own with a fifth cost status, `cost_estimated`, rather than overloading the provider's column — plus the vCPU count, because a profile moving from four to two would otherwise silently restate every historical estimate. And `billing_usage_events` had exactly one writer, a probe: the ledger every price was derived from filled up when an operator remembered, and its newest row was six days old. Usage is now projected as it is measured, with `reconcileUsage` kept as repair — no cron, no queue, rule 24's "needs no new infrastructure" met rather than argued around. Rule 78 unchanged: this is the instrument that will produce a measured Agent cost, not that measurement)
- [0074](0074-removing-a-file.md) — Removing a file: what the write path can express (Accepted; amends 0015 and 0070 in what a verified candidate may contain, and changes nothing about approval, validation or the merge. The agent has had `Bash` in its VM and `workspace_delete_file` in its compiled policy since CORE-4, so it could always delete a file; what could not happen was the last step, because `github-writer.ts` built its tree additively and the port had no entry shape that took one away. `candidate.ts` therefore refused the whole change — correctly, because a commit missing part of what the agent did is a change a human approves believing they saw all of it — and the cost was that deleting a page, a dead component or a stale route was unreachable, and a run that tried was thrown away after it had been paid for. Removal is now `{ path, blobSha: null }`, the Git data model's own shape, deliberately **not** an eighth port operation: nothing in `GitWritePort` can still amend a commit, move a ref or delete a branch. **Which paths are removed is Vibe's set difference** — the baseline listing against the workspace after the last turn — never the agent's account, which has no field in the runtime protocol and no reader (rule 77). A deletion is checked by `isAgenticWritablePath` exactly as a write is and rejected as `forbidden_path`, counts as one file against the blast-radius ceilings, and enters the candidate digest — without which a rebuild differing only in *which* files it removed would reproduce the first pass's digest. A change with no deletions digests exactly as before, so every stored `execution_identity` keeps its meaning (rule 67). **Absence is verified twice**: the post-write read-back requires every removed path to read back as nothing, and the validation sandbox's integrity check carries a null hash for it and fails on a path still present. Stored as `status: "deleted"` in `prepared_changes.files` with no hash and no byte count — a hash of the empty string would make a removed file indistinguishable from an emptied one — so the diff, the classification and the depth policy see it without being told separately; outcome verification is the one reader that holds deletions out, because a route the change deleted is supposed to be gone. The diff renders it as its base version in full, in red, **and the status comes from the stored row, never from a head read that came back empty** — `getTextFile` returns null for absent, binary and oversized alike, which is the confusion `candidate.ts` records making once. `agentic_execution_v2` rather than a redefinition, so a v1 row stays readable as a change nothing was removed by; `EXECUTION_POLICY_VERSION` does not move, because it versions what the workspace may do and that was already granted. The migration widens two constraints and the second is the trap: `prepared_changes_opportunity_required_for_generators` exempted v1 **by name**, so the bump alone would have made every v2 change unstorable — answerable only by PostgreSQL, and asserted there. No rename detection: a rename is a write plus a deletion, and calling it a rename is an inference about intent the observation cannot support)
- [0077](0077-build-chains.md) — Build chains: one run may deliver the contiguous steps of a Move (Accepted; amends 0026 and 0054, changes no risk class, path policy, tool set or merge rule. **Measured first**: over every completed plan, six runs of contiguous `vibe`/`product_change` steps — four of length two, two of length three, and **never one**. No Move has ever planned its build as a single step, so the Planner's split is readability rather than an execution boundary, and executing it as written costs run → approve → merge → re-read → possibly re-plan per step. **Not a fourth dependency class**: `classifyExecutionDependency` walks backwards and feeds `blockedBy`, so a successor there would block the run it extends; absorption is all-or-nothing where a short chain is fine; and absorbed preparation is never completed while a member must be — one type for both would break 0026's promise inside the type carrying it. So `chain.ts` walks forward, **over** the resolutions rather than inside one, because `resolvePlanExecution` maps steps independently and two screens rest on that. Rules are structural and ordered — prerequisites satisfied by completed work or the chain itself, `vibe`/`product_change`, within `MAX_AGENTIC_V1_RISK`, unserved by a registry capability, under three members — and **no prose is read**: rewording every sentence in a plan changes nothing, asserted. Three members is measured (longest observed chain; `complex` allows 12 files and run #7 took 8 for one step). Contiguous in *plan order*: a settled founder step in the middle still ends the chain, because "steps 2 and 3" is checkable against a plan and "2 and 4, skipping the decision" is not. **The identity is where money lives.** The chain enters `computeExecutionSpecIdentity` as an optional tail appended only past one member, so a solo spec hashes to what it always did — pinned by a frozen digest — because that identity feeds `computeAgentRunIdentity` and a succeeded run is *reused* by it: re-hashing would take a second reservation for delivered work. And without the keys in the hash, rule 67 breaks the other way — a declined chain's approval could come to sit on a two-step spec with the identical identity. **Neither the resolver version nor the spec schema version bumps**, argued rather than overlooked: no step resolves differently, and a v1 document read by chain-aware code yields no chain, which is what that run was. Stored as two parallel arrays so the constraints can exist (lengths agree, keys non-blank, orders strictly ascend, and **the head is always a member of its own chain**) rather than a jsonb path the SQL layer could say nothing about; empty means today's meaning, so no backfill. Completion emits one record per member sharing all four ids — the four-record rule still evaluated once per run, so a chain never turns one weak verdict into several — and the claim stays exactly as strong as 0054's. Priced over the union with the **maximum** member risk, then escalated to `complex` with a new reason `chained_delivery`: `small` and `standard` allow the same 8 files/60 KB deliberately, only `complex` widens, and selling two deliveries under a single step's ceiling would strand the run at `budget_exhausted` after payment. Stated openly: two `small` steps cost 300 apart and 350 chained; every other combination is cheaper. `EXECUTION_PRICING_CLASS_POLICY_VERSION` → v2, free because the class is deliberately not in the identity. A second trailer `Vibe-Chain`, omitted without a chain so a solo commit is byte-identical and the existing pin stays green unmodified — a repeated or comma-joined `Vibe-Step` would change the meaning of a trailer 0035 defines as one key. The screen offers two controls at two prices from one pricing function, distinguishes delivery from groundwork, and says why the chain stops; the start action takes **one boolean of intent**, never a key list, and the server re-derives members in a fresh preflight. Deliberately absent: per-member verification, chains across Moves, chains through a hard boundary, chains containing a deterministic step, any raised ceiling, a fourth tier. Untested until a real run: whether an agent given two fenced delivery blocks builds both, and whether a chain fits 12 files)
- [0078](0078-the-validation-profile-is-a-build-contract.md) — The validation profile is a build contract, not a framework list (Accepted; supersedes the profile section of Sprint 0010, amended by 0079). **The framework check changed no command**: `planValidationSteps` takes no profile and plans a locked install plus the repository's own `typecheck`/`test`/`build` scripts, so `nextjs_node_v1` never ran `next build` — it narrowed who could be checked without sharpening what the check claimed, and because `resolveExecutionValidation` asks the same function it decided who could run an agent at all. It was also **wrong in the permissive direction**: `planner-agent` declares `nextjs` from `frontend/package.json` with no root manifest and resolved `supported` at `.`, so a run would have paid for a microVM and failed in `readPlan`. `node_build_v1` is keyed on a manifest with a `build` script and a lockfile **in its own directory**, evidenced by a per-manifest detector (frameworks per manifest, never the repo-wide union; 25 targets with `truncated`; `ANALYZER_VERSION` → v5). Every refusal names the missing thing, for free, where the same refusal previously cost a provisioned VM. **`nextjs_node_v1` stays legal and resolves to nothing** — sixteen rows carry it, and no alias, because reading an old pass under today's rules is what rule 65 forbids. Two things enter the identity — the validated directory and the install commands (`sandbox-policy-v6`, `registry.yarnpkg.com`, a fourth digest line) — and wider admission does not, since a `passed` row is a statement about a transcript. Four package managers as exhaustive `Record`s; **Yarn Classic refused by name** because Yarn 1's `--frozen-lockfile` does not reliably fail on a lockfile that has fallen behind; Berry and PnP detected by file *existence* only, since `.yarnrc.yml` can carry an auth token (rule 28). The preview stops deriving from the profile and reads an ordered framework table instead; Vite is held back deliberately, because its `allowedHosts` refusal passes a loopback probe while the public URL answers "Blocked request."
- [0079](0079-the-founder-names-the-application.md) — The founder names the application, from a closed list Vibe computed (Accepted; amends 0078, grants no new authority). Once a profile admits an application rather than a repository it has to say which one, and **a workspace root becomes the directory a sandbox runs a customer's build in** — so the answer is *selected*, never constructed: `selectValidationTarget` matches by exact string equality against candidates derived from tree entries Vibe read itself, and never joins, normalizes, trims or repairs. `"../secrets"` is refused **because it is not on the list**, not because a pattern caught it; the shape is checked twice more and neither check is what makes it safe. A stored answer **routes and never permits**: one that no longer names a candidate asks again rather than reaching for the nearest survivor (rule 55), and an answer to a question nobody asked is refused as `no_choice_to_make`, because a root stored for a single-app repository would silently answer the question the day a second app appears. **A column rather than `founder_input_requests`** — that table is the pipe for open-ended prose with a supersession chain, and its CHECKs need a plan or an interrupt. Written through a **column-level grant**, not a `SECURITY DEFINER` setter: the table's UPDATE was withdrawn on purpose because the row policy would let an owner write `detached_at` over PostgREST, and `lifecycle-authority` asserts no definer function is reachable by `authenticated`. The screen offers one button per candidate and **no field**, asserted in a browser as an absence; choosing is free, reversible and starts nothing.
- [0080](0080-the-probe-that-could-not-fail.md) — The health probe carries the preview's public hostname (Accepted; amends 0078, repairs a defect that shipped with it). A preview health check ran `curl http://127.0.0.1:3000/` inside the sandbox — the right instinct, since loopback keeps every byte of an untrusted response inside the microVM. **What nobody checked is whether that question can be answered no.** Vite's host check, read from the shipped source: `if (extracted.type === "ipv4" || extracted.type === "ipv6") return true;` — every IP literal allowed unconditionally, so a loopback probe is *unable* to detect a host gate, and `healthyStatusCode` graded the 403 it would have seen as healthy anyway. Not hypothetical: 0078 shipped `astro_dev_v1` while holding Vite back for this reason, and **Astro is a Vite server**, as are Nuxt and SvelteKit. Its plan for settling it could not run either — *dogfood a Vite preview and decide*, with no Vite row, so no preview to dogfood. **Both halves close.** The server learns its own hostname through Vite's `__VITE_ADDITIONAL_SERVER_ALLOWED_HOSTS`, so no repository file is edited to make Vibe's preview work; the probe sends `Host:` on the same loopback request, so nothing leaves the VM and only the header changed — which is why the security argument survives word for word while the check stops being unfalsifiable. This needs the public origin **before** the server starts, where it was previously fetched only after a healthy probe. **A 403 is investigated, not counted**: ambiguous on its own, since an application behind auth answers it to everyone, so the two are told apart by asking twice — with the hostname and without — and different answers mean the *name* was rejected. No body is read; the block page stays discarded. `preview_host_rejected` is its own code because its remediation resembles nothing else here: the app is healthy and one line of config decides whether anyone can see it. Vite's row goes **last**, because Astro, Nuxt and SvelteKit all declare `vite` beside their own id and an earlier position would start the bare binary — working, serving the wrong thing, never looking like a bug. `PREVIEW_POLICY_VERSION` → v4, since its docblock versions health-check behaviour and "the secret policy (none)", which now has one stated exception rather than an unstated one. Depending on an underscore-prefixed, unpromised variable is acceptable precisely because its failure mode is now a named refusal rather than a silent lie. **Unproven by a real run**: no Vite preview has started in a real sandbox
- [0081](0081-routes-belong-to-an-application.md) — Routes are read relative to the application, and the router root is recorded (Accepted; extends 0078 from validation to the intelligence layer). 0078 taught the *validator* where an application lives; nothing else learned it. `detectRoutes` looked for `src/app/` and `app/` **at the repository root only**, so `planner-agent` — a real Next.js App Router project whose code is in `frontend/` — reported zero routes: not "could not read this", but an empty route table indistinguishable from a project with no router. Everything downstream inherited it — `resolveAppRoot` returned null so the **free** SEO capability refused with `unsupported_repository_layout`, the review classifier had no routes, and surface detection was weaker — which left the free half of execution **narrower than the paid half** that 0078 had just widened. The registry also promised what the button broke: `matches()` asked only the repository-wide framework union and never whether there was somewhere to write. **Shipped**: the router is looked for inside each build target (`.` when there are none, so an undescribable repository keeps today's search); two applications with routers is **refused, not resolved**, which is `resolveAppRoot`'s own rule moved to the layer that can see all of them; and `RouteIntelligence.root` is **recorded** rather than re-derived, because every consumer that rebuilt it from a source path rebuilt the root assumption with it. The field is optional and its absence is the honest answer for a pre-v6 snapshot — that analyzer only looked at the root, so the old derivation is the correct reading of its data, kept for exactly those rows. A registry match now requires a resolvable app root, checked here *and* against live state before writing (rule 55). **`ANALYZER_VERSION` → v6**, so every snapshot is stale once more — acceptable only because ADR 0080's sprint gave that state a screen that says so and links to the scan. **A fixture describing a repository that cannot exist** was found by asking: `app_router` with no root and no routes, unnoticed while the registry asked only about frameworks, six suites red the moment it asked where the application is. **Unchanged**: deterministic capabilities stay Next.js-only, and no v6 snapshot has yet been produced against a real repository
- [0082](0082-nova-voice-is-measured-not-argued.md) — Nova's voice model is chosen by eval, not by cost intuition (Accepted; introduces `nova_presentation` on the existing `AIProvider` boundary, no new authority. The audit's three-tier presentation model argued Haiku 4.5 for the middle tier on cost; the eval it names — fifty cases, a deterministic validator, an Opus 5 judge on six claims a regular expression cannot decide — found Haiku invented fluent, correctly-shaped reasons no payload gave it (`grounded` 41%, `no_invention` 39%) across two prompt revisions, while Sonnet 5 on the same prompt reached 72% and 78%. Sonnet 5 becomes `NOVA_PRESENTATION_CONFIG`; Haiku is kept as `NOVA_PRESENTATION_CANDIDATE_CONFIG`, the arm the decision was measured against, re-runnable rather than merely asserted. Safety came out model-independent — 43/46 against 42/46 `safe` — and the eval found and closed a gap in the validator itself (`"go live"` was missing from the banned-claims list). Cost was never the deciding factor: $0.0044 against $0.0014 per message, both negligible next to $0.1965 for one Business Audit)
- [0083](0083-nova-is-the-project-home.md) — Nova is the project Home (Accepted; amends 0045 and reverses the navigation half of 0047, closing §D.6 of the Nova audit. 0047 made Business Health the canonical Home because a project's most valuable answer should not be one step away — right about the diagnosis, and now answering a question the founder already asked. A project opened today is asked *what do I do now*, and a score plus nine lenses is not that answer. Nova takes `/app/projects/:projectId` and renders `deriveNovaFocus`'s primary, control, progress and secondaries; Business Health returns to the rail with a segment of its own, restoring the seven destinations 0045 chose. **The alternative is what settles it**: a seventh rail item would ask a founder arriving at a project which of seven places to go, which is the question Nova exists to remove — so a seventh item is foreclosed, now and later. `#business-audit` keeps resolving, because it is the only way out of a blocked opportunity set, and `/health` stays an alias; 0047 kept the section id stable through a label and segment change precisely so this could happen without a migration, and that affordance is now spent. Onboarding keeps its rail-free shell. Recorded with the sprint that landed Slices 2 and 3 as §O.2 requires, ahead of the rail change itself — `PROJECT_SECTIONS[0]` is still Business Health at HEAD)
- [0075](0075-the-photograph-nobody-took.md) — The photograph nobody took: the visual review capture path is deleted (Accepted; completes 0065, which decided this and deferred it. 0065 wrote down its own condition — *deletion is a later slice, once the last artifact has passed its seven-day retention* — and that condition is met: the newest `review_artifacts` row is 2026-08-14, nineteen days old and twelve past. **Three counts read from the database decided what may not go.** One `change_approvals` row carries a `review_artifact_id`, so the read path and its authorization tests stay — rule 67 binds an approval to an immutable artifact identity, and an approval whose evidence cannot be displayed is one nobody can audit. Two `operation_runs` rows carry `change_review`, so the type stays in `OperationType` and in the SQL CHECK, mapped to a `retiredWorkflow` that **throws** — resolving successfully would leave a row that never finishes on an operation nothing should have started. And `sweepExpiredReviewScreenshots` still has a caller in preview teardown: retention is not the capture path, it is what empties the bucket. 2,041 lines removed, 66 added; the test count falls 7,379 → 7,355, which is the correct direction — what those 24 asserted was the behaviour of a path with no caller. `review_browser_usage` keeps its rows and loses its writer, `provider_cost_usd` still null in every one, because inventing a Browserbase rate to close a column is what `margin-guard.ts` refuses by design. `@browserbasehq/sdk` and `playwright-core` stay production dependencies — they are the Deep Scan's under 0012 and always were, and CLAUDE.md rule 38 is rewritten in place to stop citing a deleted path as its justification. Claims nothing about the surviving read path being exercised: the one approval that depends on it is fifteen days old)
- [0076](0076-the-browser-we-own.md) — The browser we own: Deep Scan runs in Vibe's own sandbox (Accepted; supersedes 0012 in its **choice of provider only** — everything 0012 decided about how an authenticated analysis may work is unchanged and is what this implements. **The plan it started from does not work, and the reason is not visible from outside the code**: reusing the *preview* sandbox would have put a person in front of the customer's repository running with no environment at all — `provisionPreviewWorkspace` refuses a privileged environment before it creates anything — so no database, no auth keys, no accounts, and every login fails. The Deep Scan points at the customer's own production origin, which is the thing a preview cannot be. **The economics were not what the plan assumed either**, measured before writing anything: 5 browser sessions totalling 190 seconds and 7 analysis runs totalling 3.6 minutes across all of production, so per-scan browser time is close to free at anyone's rate and only ending a subscription saves money. **`BrowserSessionProvider` did not change** — the analyzer, its read-only policy, the route budgets, entitlement and billing all sit above it untouched, which is the evidence the boundary was drawn in the right place and the reason its docblock now says so rather than reading as inevitable. A third `SandboxSource`, `image`, carries no clone, no credential and no customer filesystem, which is what makes the rest defensible rather than a series of exceptions. Chromium's DevTools endpoint has no authentication and never will, so it stays on loopback behind a Vibe-authored guard on the one public port, with **two** capability tokens: `control` is a byte pipe to CDP and never leaves the server; `view` travels to a browser and is deliberately not a pipe — four message shapes translated into five named CDP calls, so a leaked view token can click and type in a browser already showing the owner's session and nothing more. Strictly less than the DevTools frontend with an address bar it replaces, which ran the customer's signed-in application inside an iframe needing `allow-scripts` and `allow-same-origin` together; what arrives now is a JPEG on a canvas, which executes nothing. **The tokens are derived rather than random or stored**, and the reason belongs to the flow rather than to cryptography: the manual-login lifecycle spans two server requests, and storing a token would put a bearer credential for a browser already signed into a customer's product into a database row — the exact artefact 0012 declined to hold. **`allow_all` egress exists for this one caller** because a login's destinations cannot be enumerated in advance, and `network-policy-scope.test.ts` fails the build if anything else names it, if a permitted entry stops using it, or if an entry's reason does not name an ADR — each entry must argue what is *in* the sandbox, since egress matters exactly when there is something to exfiltrate. Rule 64 is untouched for anything running customer code. Build egress and session egress stay separate windows. The image is a snapshot keyed on the guard version, rebuilt lazily by the next read that finds none — no cron, no queue. **Deep Scan stops being the price with no measured cost**: `terminateSession` returned `Promise<void>` and discarded the figures at the one moment they exist, and now reports them into columns of their own under `cost_estimated`, $0.0007–$0.0116 against $0.441 of revenue. **Records what it gives up**: no fallback, since direct replacement was chosen over parallel running; an interactive view Vibe now maintains, where a DevTools frontend handled popups, IME and clipboard and four message shapes do not; and build commands asserted in shape but never run in a real sandbox)

Several architecture questions remain intentionally open — see [ARCHITECTURE.md §7](../../ARCHITECTURE.md#7-deferred--open-decisions).

## Suggested format

```
# NNNN - Title

Status: Proposed | Accepted | Superseded by NNNN
Date: YYYY-MM-DD

## Context
What problem or question this decision addresses.

## Decision
What was decided.

## Consequences
What this makes easier, harder, or forecloses.
```
