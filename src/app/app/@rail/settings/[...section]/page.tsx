/**
 * Every page under Settings, matched so the rail layout above stays mounted
 * across all of them. Renders nothing: the rail is the layout, and this is
 * what keeps it still.
 */
export default function SettingsRailSectionPage() {
  return null;
}
