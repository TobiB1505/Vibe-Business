/**
 * Every section under a product — `plan`, `agent`, `settings/activity` and the
 * rest — matched so the rail layout above stays mounted across all of them.
 * Renders nothing: the rail is the layout, and this is what keeps it still.
 */
export default function ProjectRailSectionPage() {
  return null;
}
