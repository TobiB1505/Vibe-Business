/**
 * The rail is the layout beside this file; this matches the product's index so
 * that layout is reached. It renders nothing on purpose — a slot page that
 * drew anything would be a second rail underneath the first.
 */
export default function ProjectRailPage() {
  return null;
}
