/**
 * The application's own title and description, in one place so any page
 * that needs to reuse them — for Open Graph tags, for example — reads the
 * same values the root layout already sets rather than duplicating them as
 * a second, independently-editable literal.
 */
export const SITE_TITLE = "Vibe Business";
export const SITE_DESCRIPTION = "The business layer for AI-built products.";
